/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: "/portfolio-react",
  output: "export",
  reactStrictMode: true,
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
